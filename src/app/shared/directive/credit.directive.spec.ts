import { CreditDirective } from './credit.directive';

describe('CreditDirective', () => {
  it('should create an instance', () => {
    const directive = new CreditDirective();
    expect(directive).toBeTruthy();
  });
});
